# 在线演示

你可以在下面的演示区测试 SQL 编辑、主题切换、尺寸控制、`v-model` 绑定，以及库、表、字段补全。侧边栏可切换示例场景，输入 `.` 或空格触发库、表、字段补全。

编辑器默认跟随页面主题，也可以随时通过工具栏手动覆盖。

<ClientOnly>
  <div class="demo-doc-block">
    <AppDemo embedded />
  </div>
</ClientOnly>

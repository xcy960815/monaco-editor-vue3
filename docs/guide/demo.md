# Online Demo

Use the playground below to test SQL editing, theme switching, layout controls, `v-model` binding, and database/table/field completion. Pick a scenario from the sidebar to load a sample, and type `.` or a space to trigger database, table, and field completion.

The editor theme follows the page theme by default; override it any time from the toolbar.

<ClientOnly>
  <div class="demo-doc-block">
    <AppDemo embedded />
  </div>
</ClientOnly>
